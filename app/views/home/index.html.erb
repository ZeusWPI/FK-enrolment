<h2>Welkom</h2>

<p>Kies de kring waarbij je je wil inschrijven.</p>

<table class="shield-picker"><tr>
<% @clubs.each_with_index do |club, i| %>
  <%= raw("</tr><tr>") if i > 0 && i % 4 == 0 %>
  <td><%= link_to image_tag(club.shield_path(:small)), registration_root_path(club) %>
  </a></td>
<% end %>
</tr></table>
