<%= f.input :value , :as => :check_boxes,
    :label => f.object.spec.name, :collection => f.object.spec.values,
    :wrapper_html => { :class => "checkbox-grid no-indent" },
    :hidden_fields => false %>
